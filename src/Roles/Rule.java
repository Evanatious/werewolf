package Roles;

/** An interface to represent a Roles.Rule (usually alternative) in ONUW. This
 *  interface is intentionally left blank; its purpose is to have other
 *  classes/enums implement it so that they can be identified as a "rule".
 *
 * @author Evan Gao
 */
public interface Rule {
}
